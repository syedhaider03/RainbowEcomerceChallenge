import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  loader: {
    width: '100%',
    height: '100%',
    position: 'absolute',
    zIndex: 1,
  },
});

export default styles;